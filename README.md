# 奕霖古诗词 · 诗词卡片美化

把中国古典诗词一键变成可分享的精美图片，并可交给大模型生成配图底图。

纯前端，零后端，无需登录。审美是第一优先级。

**在线体验** → <https://text2card-q3j.pages.dev/>

![奕霖古诗词 界面](assets/screenshot-app.png)

![诗词卡片（16:9 竖排）](assets/sample-poetry.png)

---

## 这个项目已收敛为诗词专用

顶栏原有的「自动 / 代码 / 金句 / 长文 / 诗词」风格 chips 已移除，风格固定为**诗词**。

多风格的机制仍然完整保留在代码里（`renderCard` 的 switch、四张卡片组件、
各自的主题表、`classifier.ts`），只是不再有 UI 入口——因为还牵涉主题表、
分类器与快照 schema，直接删掉是不可逆的大改动，所以先只切断入口。
想恢复多风格：把 `App.tsx` 里的 `FIXED_STYLE` 常量改回 state，再把 chips 加回去即可。

因此下文仍保留各风格的说明，作为代码参考。

---

## 特性

- **中文古典诗词卡片**（竖排宣纸，自动从《》和 —— 提取标题作者；
  印章与标点符号各有开关，均默认不显示）
- **保存与「管理」**：一键把当前诗词、背景、AI 背景图和全部预览设置存成快照；
  独立的管理页可网格浏览、检索排序、多选、删除，并导出单张 PNG / 背景原图 / 批量 ZIP。
  **「标题 + 作者」相同即更新**，不会攒重复
- **保存不设条数上限**，并显示存储占用；写满时明确报错而不是悄悄删掉旧数据
- **可换字体**：中文 24 档 + 西文 10 档共 34 档，分组可折叠，
  作用到正文与标题；**字号**可在 70%–150% 之间无级调节
- **AI 背景图**：把诗词拆成 主体 / 环境 / 风格 / 光影 / 构图 五个独立字段，
  逐字段可改，再拼成提示词交给大模型出图，作为卡片底图（见下文）
- **3 种导出尺寸**：自适应（宽 1080）/ 3:4 竖图（1080×1440）/ 16:9 横图（1920×1080）
- **一键导出 PNG**，`pixelRatio: 3`，分享到手机不糊
- **纯前端 · 隐私**：渲染与导出全部在你的浏览器里完成，卡片渲染不依赖任何服务端。
  唯一会联网的功能是 **AI 背景图**，且只在你主动点「自动拆解 / 生成背景」时才把
  诗词正文或提示词发给模型；不用这个功能就完全离线

### 代码里保留的其余三套卡片（当前无入口）

- 代码（Carbon 风暗色 IDE，6 个 VS Code 主题）
- 金句（大字海报，4 套柔和渐变 + 装饰引号）
- 长文 Markdown（杂志风，3 套配色，标题/列表/引用/代码块全自定义渲染）

以及内容启发式分类器 `classifier.ts`（粘贴即识别风格，曾是 chips 上 `自动 · 长文`
判定的来源）。它们都还在、也都还有测试覆盖，只是现在没有入口。

---

## 运行

要求 **Node.js ≥ 23.4**（数据层用 Node 内置的 `node:sqlite`，23.4 起才不需要实验开关），
macOS / Windows / Linux 均可。

```bash
npm install
npm run app      # 起本地服务 + 自动打开浏览器（推荐）
```

Windows 上还可以直接双击项目根目录的 **`startup.cmd`**，效果一样。

也可以只用 `npm run dev` 起服务，再自己打开 <http://localhost:5173/>。

**为什么必须有个本地服务**：卡片、音频、配图、字幕都存在项目里的 `data/` 目录
（一个 `library.sqlite` + `audio/` + `images/`），由本机 Node 服务提供。所以纯静态托管
（比如 Cloudflare Pages）只能看到界面，看不到自己的库——界面上会明确提示这一点，
而不是显示一个空库。

服务起来后终端会打印两个地址：本机地址，以及**同一 WiFi 下手机/平板可访问的局域网地址**。

导出 PNG 建议使用 Chrome / Edge 等
Chromium 系浏览器——html-to-image 的 DOM→SVG→canvas 渲染路径在 Safari
上有已知 bug（字体丢失、图像空白）。

构建生产版本：

```bash
npm run build
npm run preview
```

### 数据在磁盘上长什么样

```
data/
  library.sqlite     卡片元信息、标签、墨色、歌单、预设（几百 KB，不含图片音频本体）
  audio/<卡片id>.<ext>   音频原文件，可以直接用别的播放器打开
  images/<卡片id>.<ext>  AI 配图原图（草稿配图是 draft.<ext>）
```

**体积大的东西一律不进数据库**：一张 AI 配图 1–3MB、内联成 base64 还会再胖 1/3，
塞进 SQLite 行里会让每次读一张卡片都要解析几 MB 的 JSON、改一个字也要重写整行。
所以库里只留 `{ scrim, bytes, ext }`，图在 `images/` 下按卡片 id 命名；
管理页列表只下发 240px 缩略图，要看大图／导出／备份时才按需取原图。

整个 `data/` 目录可以直接拷走、也可以只备份 `library.sqlite`+两个文件夹（界面上
「备份」导出的 ZIP 就是这三样）。

---

## AI 背景图（诗词卡）

把一首诗变成「五字段 → 提示词 → 背景图」，作为诗词卡的底图。

### 配置密钥

图像与文本都走**火山方舟**，一套 `ARK_API_KEY` 同时用于拆解与出图。

本地开发：

```bash
cp .env.example .env      # 然后填入 ARK_API_KEY
```

`.env` 已在 `.gitignore` 里。密钥只被 Node 进程读取，**不会进前端 bundle**
（构建产物里检索 `ARK_API_KEY` / `ark.cn-beijing` 均为 0 命中）。

线上（Cloudflare Pages）：在项目的
**Settings → Environment variables** 里加一个 `ARK_API_KEY` 即可。生产环境由
`functions/api/*.ts`（Pages Functions）承接同样的接口。

### 使用流程

1. 左侧粘贴诗词，顶栏切到 **诗词**（或让它自动识别）
2. 右侧「AI 背景」→ 点 **自动拆解**：文本模型输出五个字段并填入输入框
3. 按需手改任一字段——提示词始终以面板内容为准，自动拆解只是给个起点
4. 展开 **提示词预览** 可以看/复制最终提示词
5. 点 **生成背景**：出图后自动铺为卡片底图，并可调「蒙层」浓淡
6. 正常导出/复制 PNG，背景图会一起导出

### 五个字段

| 字段 | 职责 | 示例 |
|---|---|---|
| 主体 | 画面唯一核心实体 | 一叶孤舟 |
| 环境 | 时空、天气与陪衬景物 | 寒江暮色，远山薄雾 |
| 风格 | 绘画媒介与流派 | 水墨写意，淡彩渲染 |
| 光影 | 光源方向与色调 | 月华清冷，侧逆光 |
| 构图 | 取景视角与留白 | 竖幅三分法，左侧大片留白 |

拆解与拼装共用 `shared/scene.ts`：服务端按同一份字段元数据生成结构化输出要求，
前端按同一份元数据拼提示词，两边不会漂移。

### 两个设计取舍

- **蒙层复用主题自己的渐变**，而不是加一层黑色遮罩。照片退成一层「淡底画」，
  文字颜色照旧由主题的 `text` 决定——对比度不依赖对图片做明暗分析，换主题时
  整张卡的观感也跟着变。蒙层**默认 50%**，滑杆可在 0–90% 之间调。
- **图片以 data URL 内联**，不用远程 URL。远程图片会污染导出用的 canvas
  （`toBlob` 直接抛安全异常），而且会被方舟 CDN 的 CORS 挡掉；服务端回源取字节
  后转 data URL，浏览器侧永远是同源数据。

### 可选环境变量

全部可留空，用代码内默认值。报「模型不存在 / 无权限」时优先改前两个：

| 变量 | 默认 | 说明 |
|---|---|---|
| `ARK_TEXT_MODEL` | `doubao-seed-2-1-pro-260915` | 拆解用文本模型 |
| `ARK_IMAGE_MODEL` | `doubao-seedream-4-0-250828` | 出图模型 |
| `ARK_IMAGE_SIZE` | `2K` | Seedream 4.x/5.x 用 `1K`/`2K`/`4K`；实测 2K + 竖版提示词出 1728×2304 |
| `ARK_IMAGE_RESPONSE_FORMAT` | `b64_json` | 换成 `url` 则服务端回源下载后再转内联 |
| `ARK_BASE_URL` | 方舟北京区 | 走自建网关时改 |

> 默认模型 ID 是实测返回 200 后才写进代码的，但**模型可用性随账号与开通状态而变**：
> 方舟要求先在控制台开通模型，未开通时同一 ID 会返回
> `InvalidEndpointOrModel.NotFound` 或 `ModelNotOpen`。这两类报错都带方舟原始
> Request ID 并直接显示在界面上，照着换成你账号可用的 ID 即可。
> 查询方式：`GET {ARK_BASE_URL}/models`（注意 `status: Shutdown` 的是已下线模型，
> 列出来也调不通）。
>
> 出图命中 400 时，服务端会自动摘掉 `response_format` /
> `sequential_image_generation` / `stream` 重试一次——但**不会**摘掉
> `watermark: false`，宁可失败也不接受一张烧了水印的卡片背景。

### 已知限制

- 生成的背景图**不写入 localStorage**（动辄数 MB，会撑爆配额），而是单独存
  在 IndexedDB 的 `draft` 仓库里，所以**刷新页面不会丢**；五个字段本身走
  localStorage 自动保存
- 出图尺寸默认 `2K`，而导出是 1080 宽 × `pixelRatio: 3` = 3240px，
  背景会被放大，追求极致锐度可设 `ARK_IMAGE_SIZE=4K`
- 背景图目前只作用于诗词卡

---

## 管理页（查看 / 删除 / 导出）

顶栏有 **编辑** 与 **管理** 两个平级页面，用 hash 路由（`#/edit` / `#/library`），
所以刷新后还停在管理页、链接也能分享；静态托管不会像 history 路由那样 404。

两页的职责边界：**管理页不就地改内容**——改内容回编辑页（避免两处编辑逻辑分叉）。
「载入到编辑」是两页之间唯一的正向通道，配合「标题 + 作者」相同的 upsert 规则，
正好形成「选一条 → 改 → 存回来更新同一条」的闭环。

### 管理页有什么

- **网格 / 列表两种视图**。网格里的缩略图是**真实卡片的等比缩小版**——复用同一份
  `SnapshotCard`（`ScaledCard` 按列宽缩放），所以每张都忠实显示"自己的诗词 + 自己的底色"，
  连正文排版（竖排/横排、字体、字号）都一致
- **详情灯箱**：点卡片从**屏幕中央**浮出大图（不是右侧抽屉——点卡片就是为了看图，
  居中才够大），下方是「载入到编辑页 / 导出 PNG / 导出背景 / 删除」，
  元信息默认收起，点「详细信息」才展开
- **检索**：标题 / 作者 / 正文全文包含匹配
- **排序**：最近更新 / 创建时间 / 标题
- **多选**：悬停出现勾选框，底部工具条支持全选、批量导出、批量删除、清空全部
- **存储占用**：`navigator.storage.estimate()` 显示已用 / 配额，并在未授权时提示
  申请持久化存储

### 缩略图为什么是「迷你卡片」而不是「背景图」

网格里的缩略图**不是**把背景图缩一份贴上去，而是用同一份 `SnapshotCard` 按列宽
等比缩放的**真实卡片**。这不只是好看：

早先的做法是"背景图回声"，结果存过 AI 配图的卡片显示的是一张照片、没配图的显示
一块纯色渐变，两者观感天差地别——按「最近更新」排序时，恰好最近保存的那张若没有
配图，就会显得"跟其余的不是一种东西"。（这就是"为什么第一张和后面不一样"的成因。）
换成迷你卡片后，每张都只差在底色上，且正文排版、字体、竖排方向全部如实还原。

代价控制：缩略图仍用保存时生成的 **240px JPEG**（实测约 3KB），**绝不用原图**——
一张原图 1–5MB，几十张同时挂进 DOM 会吃光内存。老记录（此功能之前保存的、没有
`thumb`）刻意**不回落到原图**，宁可显示主题渐变也不让网格变卡。

### 导出：三种粒度与实测代价

| 粒度 | 产物 | 说明 |
|---|---|---|
| 单张 PNG | 复用 `exporter.ts`，`pixelRatio: 3` | **实测 1.9 秒/张** |
| 单张背景 | 原始 AI 配图 | 素材复用 |
| 批量 ZIP | 选中的多张打包 | **实测约 10MB/张**；2 张 = 19.9MB |

批量导出是**离屏渲染**：把 `SnapshotCard` 挂到屏幕外（`left:-100000px`，
**不能用 `display:none`/`visibility:hidden`**，否则 html-to-image 克隆出的 SVG 会是白图），
渲染完再走同一条栅格化管线。因为单张要 1.5–2 秒，所以带进度条、累计体积与取消；
选中 ≥8 张时会先给出「预计约 XXX、耗时约 YY 秒」让你确认再动手。

ZIP 用自写的 **store-only 打包器**（`lib/zip.ts`）：PNG 本身已 deflate 过，再压一遍
几乎不减小体积，所以只需要打包不需要压缩，store-only 的 ZIP 结构不到 100 行、
零依赖。产物已用 .NET `ZipFile` 解压验证（含中文文件名）。

### 「最近」弹层与管理页的关系

**并存，分工明确**：弹层是编辑页里的**快速切图器**，只显示最近 20 条（`RECENT_LIMIT`，
**不是保存上限**），底部有「在管理页查看全部」入口；完整列表、检索、多选、批量导出
都在管理页。两者共用同一套 store hook，不重复实现删除/载入。

---

## 保存与「最近」

顶栏 **保存** 把当前全部界面状态存成一条快照：诗词正文、风格、尺寸、
主题、标题/作者/眉头、竖排/横排、留白、**字体**、五个画面字段、
以及**AI 背景图与蒙层浓度**。顶栏 **最近** 打开列表，最多保留 20 条，
点任意一条即可整套载回主界面，也可单条删除或全部清空。

### 为什么快照走 IndexedDB 而不是 localStorage

一张 AI 背景图的 data URL 就有 1MB 上下，而 localStorage 配额通常只有 5MB，
存两三次就会把整个站点的自动保存写崩——连草稿都保不住。所以两者分工明确：

| 通道 | 存什么 | 上限 |
|---|---|---|
| `localStorage` | 当前工作草稿（**不含**背景图，防撑爆） | 1 份，自动保存 |
| `IndexedDB` · `draft` 仓 | 当前背景图（含蒙层浓度） | 1 份，改动后防抖写回 |
| `IndexedDB` · `snapshots` 仓 | 显式保存的快照（**含**背景图） | 20 份 |

三者的分工来自一个硬约束：一张 AI 背景的 data URL 有 1–5MB，而 localStorage
配额通常只有 5MB——写进去会把整个草稿保存一起搞崩。所以「小字段走 localStorage
自动保存、大图走 IndexedDB」是必须的。

`draft` 仓是后补的：一开始只有前两者，结果是**「从『最近』载入快照 → 刷新页面」
背景会丢**——刷新后只恢复 localStorage 的草稿，而草稿里没有图。现在背景图会
跟着当前状态单独落盘，刷新后读回。

写入做了 500ms 防抖，否则拖蒙层滑杆时每一帧都会写一次几 MB 的记录。

配额写满时不会直接报错：会淘汰最旧的一条再重试（最多 3 次），
避免「存到某个体积后就再也存不进新内容」这种没有自救手段的死局。

### 保存的键是「标题 + 作者」

同一个「标题 + 作者」再次保存时是**更新**，不是新增：内容整体替换、时间戳刷新
（于是冒到列表最前），条目数不变。这样同一首诗反复改、反复存不会攒出一堆重复
条目、白白吃掉 20 条上限。改了标题或改了作者，就是另一条。

**标题为空时不能拿空串当键** —— 那样所有「没填标题」的内容会挤在同一格，
后存的把先存的覆盖掉。所以标题为空时退回 `labelOf()` 的结果：正文里《》的
题目 → 正文首行。首行是内容的一部分，不同内容的键天然不同，而同一篇反复保存
仍然稳定命中同一条。实测：两首都没填标题的诗各自成条、互不覆盖；同一首存两次
则更新。

命中更新时提示会明确说「标题与作者相同，已更新「江雪」」，不会让人以为又存了一条。

### 一个容易踩的坑

载入快照时必须提前把「上一次的生效风格」对齐到恢复后的风格。否则
`setStyleChoice` 引起的风格变化会让「风格变了就重置主题索引」那个 effect
在本次渲染后触发，把刚恢复的 `themeIndex` 清成 0——表现就是
「载入后主题总是回到第一个」。见 `App.tsx` 的 `handleRestore`。

---

## 字号

右侧「字号」是一根滑杆，范围 **70%–150%**，作用范围与字体一致（**正文与标题**），
署名、眉头、文件名栏、印章不参与缩放。旁边非 100% 时会出现「复位」。

倍率是在各卡片**自己算好的字号**上再乘的——卡片本来就会按文字长度自动挑字号
（短句更大、长文更小），滑杆是在这个基础上微调，不是覆盖它。

上限刻意只到 150%：卡片高度是固定的（3:4 / 16:9），字号放大必然溢出被裁。
与其让用户撞上截断提示，不如把范围收在「一般还装得下」的区间；真需要更大，
应该配合「尺寸 → 自适应」一起用（自适应会把卡片高度撑开）。

### 为什么放大字号时背景会跟着「动」

这是**自适应尺寸**下的固有现象，而且是两个效应叠加，实测数据（配图用实际的
2K 竖版 1728×2304）：

| 尺寸 | 字号 | 卡片高 | 预览缩放 | cover 倍率 | 居中时可见的图像范围 |
|---|---|---|---|---|---|
| 自适应 | 100% | 979 | 70% | **0.6250** | 图 y 369 → 1935 |
| 自适应 | 120% | 1086 | 63% | **0.6250** | 图 y 283 → 2021 |
| 3 : 4 | 任意 | 1440 | 47% | 0.6250 | 图 y 0 → 2304（整张） |

**效应一：预览整体缩放。** 自适应下卡片高度随内容增长（979→1086），预览为了
把整张卡塞进视口会等比缩小（70%→63%），于是背景图、文字、印章**一起**变小。

**效应二：取景窗口平移。** 卡片长高 → 背景层（`inset:0`）跟着长高 →
`background-size: cover` 重新计算。因为「卡高/卡宽」仍小于「图高/图宽」，
**cover 倍率始终是 0.6250，图本身没有被缩放**；但居中裁切的窗口变了：
可见范围从图 y 369 起变为 y 283 起，**图像整体上移了 86 像素**。
这就是「背景跟着动」的来源——不是图被放大缩小，而是取景框在动。

**关键结论：用 3 : 4。**

竖版 2K 出图实测正好是 **1728×2304 = 3:4**，而 3:4 卡片是 1080×1440，
两者宽高比完全一致 → `cover` 算出来的渲染尺寸恰好等于卡片尺寸，
**一点不裁、一点不缩**。实测在 3:4 下把字号从 100% 拉到 150%，
背景的可见范围始终是整张图（y 0 → 2304），**纹丝不动**。

所以：**要 AI 背景就用 3:4**；自适应模式适合没有背景图、或不在意取景变化时。

> 顺带说明：导出走 `node.offsetWidth/Height`（卡片真实尺寸），不受预览缩放
> 影响。预览右下角的 `1080 × 1086 · 预览 63%` 读数就是给这件事做说明的。

## 字体

右侧面板「字体」分**中文 / 西文**两组、共 34 档（中文 24 + 西文 10），
分组可折叠（初始只展开当前选中项所在那一组；折叠时组标题右侧会显示选中的
是哪一档）。全部 self-host，不产生任何外部请求（否则导出的 PNG 会因字体
未加载而回退系统字体，屏幕预览与导出结果就不一致了）：

| 组 | 档位 |
|---|---|
| **中文** | 默认 · 宋体（Noto Serif SC） · 文楷（LXGW WenKai） · 黑体（Noto Sans SC） · 晰黑（LXGW Neo XiHei） · 楷书（Ma Shan Zheng） · 楷体（FZKai-Z03） · 仿宋（FZFangSong-Z02S） · 大楷（Alimama DongFangDaKai） · 小薇（ZCOOL XiaoWei） · 珠圆（MaokenZhuyuanTi） · 快乐（ZCOOL KuaiLe） · 庆科（ZCOOL QingKe HuangYou） · 得意（Smiley Sans） · 无锋（MDMD-WuFengTi） · 龙珠（LogoSC LongZhuTi） · 龙藏（Long Cang） · 悠哉（Yozai） · 小赖（Xiaolai SC） · 行书（Zhi Mang Xing） · 弘雷（hongleixingshu） · 拙书（HongLeiZhuoShu） · 草书（Liu Jian Mao Cao） · 甲骨（FZJiaGuWen） |
| **西文** | 等宽（JetBrains Mono） · Plex（IBM Plex Mono） · Fraunces · Garamond（EB Garamond） · Cormorant（Cormorant Garamond） · Lora · Playfair（Playfair Display） · Manrope · Caveat · Vibes（Great Vibes） |

作用范围是**正文与标题**；署名、眉头标签、文件名栏这类装饰性文字
保持原设计，避免整张卡失去层次。

> 日文那两组（Klee One / Yuji Syuku）已经移除——它们虽是汉字，但字形是
> 日式的（「直」「骨」「今」写法与简体不同），简体专用字还要回落，对这个
> 项目没什么用。

### 两个「选了没反应」的坑

这两个都不是 bug，但不说明白就会被当成 bug：

- **宋体 = 诗词正文的默认字体**，在诗词卡上选它当然看不出变化
  （在金句 / 长文 / 代码卡上是有变化的，那些卡的默认字体不是宋体）。
- **4 个西文选项没有中文字形**，正文是中文时会回落到后面的中文字体。
  面板上选到这些会显示提示。

判断「换字体有没有生效」不能看排版尺寸：中日韩字形在所有字体里都是
**全角等宽**，换字体只改字形、不改宽度。要靠字形本身比对。

### 按需加载

可选字体的 CSS **不静态引入**。它们的 `@font-face` 规则合计 600 多条
（每个中文字体按 unicode-range 切 90–100 片），全量引入会把 CSS 从
266KB 顶到 1086KB（gzip 107→490KB），而绝大多数访客只会用默认那几套。

所以 `lib/fonts.ts` 里的 `ensureFontLoaded()` 在**选中时**才动态 import
对应 CSS，Vite 会为每个包单独切一个 chunk：默认 CSS 保持 269KB，
8 个可选字体各是一个 105–152KB 的独立文件，不选就不下载。字体文件本身
同样只下载用到的分片。

唯一代价是首次选中时有一瞬间的回退字形（`font-display: swap`）。

### 加新字体时要当心

**不是所有中文字体包都能直接用。** 加字体时踩过两个坑：

**坑一：包可能完全没分片。** `@fontsource/lxgw-wenkai` 没有按 unicode-range
切片，单个 woff2 就有 **6.9–8.4MB**（而且文件名叫 `latin` 却装了整套中日韩
字形，标签是错的），还缺 400 字重。改用官方网页版 `lxgw-wenkai-webfont`：
582 个分片、最大单片 60KB。

**坑二（更隐蔽）：「最大单片小」不等于「下载少」。** 实测一首 20 字诗真正
会拉取的分片量：

| 字体 | 分片数 | 单片最大 | **一首诗实际要下** |
|---|---|---|---|
| 站酷小薇 / 龙藏 | 12 | 0.08 MB | **0.77 MB** |
| 霞鹜文楷 | 97 | 0.06 MB | **4.42 MB** |
| Klee One | 17 | 0.05 MB | 0.37 MB |
| Yuji Syuku | 17 | 0.06 MB | 0.53 MB |

文楷的单片最小，但因为切得太细，一首诗要拉 97 片、比站酷系列重 6 倍。
**判断字体贵不贵要看「命中多少片 × 每片多大」。**

**坑三：有些字体看起来有用，其实字形完全相同。** 曾加过一档「繁宋」
（`@fontsource/noto-serif-tc`），实测后删掉了——思源宋体的 SC 与 TC 是同一套
字形设计，逐像素比对在简体诗、繁体诗、以及「直骨今画」这类区域差异字上
**全部一模一样**。加进去只是个「选了没反应」的摆设。

所以加字体前至少确认三件事：**有没有 `unicode-range` 分片**、
**实测一首诗要下多少**、**换上去是否真的改变了字形**。

### 授权情况（商用前请看）

新增的中文字体来自两类来源，**授权清晰度不一样**：

| 授权 | 字体 |
|---|---|
| **SIL OFL 1.1（开源，可商用）** | 晰黑（LXGW Neo XiHei）、悠哉（Yozai）、小赖（Xiaolai SC）、龙珠（LogoSC LongZhuTi）、得意黑（Smiley Sans，上游为 OFL） |
| **作者声明免费商用，但包内 `license` 字段为 unknown** | 大楷（阿里妈妈东方大楷）、楷体（方正楷体）、仿宋（方正仿宋）、弘雷、拙书、珠圆、无锋 |
| **授权最不明确** | 甲骨（方正甲骨文）——方正字体商用需按其授权条款确认 |

`cn-fontsource-*` 这一系列包的元数据里 `license` 大多写 `unknown`，只在
`font.type` 标了 `free`，并链到猫啃网的介绍页。**要商用/公开发布前，请按
版权方页面逐项确认**；纯自用则无碍。要我把上表后两类直接删掉，说一声即可。

```bash
# 1) 分片数与 unicode-range 条数应当相等
grep -c 'unicode-range' node_modules/<包名>/400.css
# 2) 最大的分片
ls -S node_modules/<包名>/files | head -1
# 3) 字形是否真的不同：用 canvas 逐像素签名比对（中文字形全角等宽，
#    量宽度是量不出区别的——六种字体宽度会完全一样）
```

这个区块刻意排在右栏**最末**。右栏是 `overflow-y-auto` 的窄栏，任何插在
中间的新区块都会把它下面所有内容往下推：它一度被放在「主题」后面，
结果把「AI 背景」挤出首屏，看起来就像那个功能消失了。
新增区块请往末尾追加。

代码卡略微特殊：`shiki` 会给自己生成的元素加内联样式，而
`styles/index.css` 里那条 `.shiki { font-family: ... !important }` 会压掉
组件上的 `fontFamily`，所以代码字体是通过 CSS 变量 `--code-font` 注入的
（未设置时回退 JetBrains Mono）。

---

## 使用

1. 左侧粘贴诗词（或点左侧顶部的 `示例：五言 / 七言 / 宋词` 载入）
2. 左侧底部填 **标题 / 作者**，或点 **解析题目 / 作者** 从原文里自动拆出来
3. 中间实时预览（手机端首次默认 3:4 竖图）
4. 右侧「调整」面板选尺寸 / 主题 / 排版 / 字体 / 字号，以及 AI 背景
5. 右上角 **复制图片** 或 **导出 PNG**（⌘/Ctrl+S）；**保存** 把当前状态存进「最近」

### 各卡片识别规则（当前无入口，保留备查）

曾由 `classifier.ts` 在粘贴时自动判定风格。风格已固定为诗词，这段逻辑不再被调用：

| 内容形态 | 自动归类 |
|---|---|
| 含 \`\`\` 代码围栏，或前几行多为 `function/class/import/<...>` 等 | **代码** |
| 短句 ≥ 4 句且**句长完全一致**、长度属于 4/5/7 字（四言、五言、七言），或含《标题》 | **诗词** |
| 含 Markdown 语法（`#`、`-`、`>`、表格 等）或 > 300 字 | **长文** |
| 短句、有引号包裹、≤ 140 字 | **金句** |

**诗词的判定偏严，这是刻意的。** 只要求「每句 4-9 字」会把现代文案也判成诗词
（「愿你慢慢长大，愿你有好运气…」句长 6/6/4/11），所以额外要求句长整齐且
属于古典常见句长。代价是几类内容认不出来，需要手动点「诗词」：

- **宋词、古体诗**：句长本就不整齐（如「大江东去，浪淘尽，千古风流人物」）
- **只有两句的短诗**：与两句现代短句无法区分（如「人生若只如初见，何事秋风悲画扇？」）
- **六言诗**：少见，为挡住「全是 6 字的现代文案」而未纳入

> ⚠️ 这一点很重要：**AI 背景面板只在生效风格是「诗词」时渲染**。
> 风格选「自动」时由上面的分类器决定，判成别的风格，那个面板就不会出现
> ——看起来就像功能消失了。手动点一下顶栏的「诗词」chip 即可。

代码围栏的语言标记支持常见简写（\`\`\`js、\`\`\`py、\`\`\`sh 等），写了无效语言时会自动回退到代码内容检测。

### 各卡片支持的元数据

这些字段都在**左侧编辑栏底部**，紧贴正文——它们本来就是从正文里拆出来的，
放在一起才好对照。（只有长文的「眉头标签」留在右侧面板，它属于版式而非内容。）

| 卡片 | 字段 |
|---|---|
| 代码 | 文件名（不填会按语言自动猜 `snippet.ts` 之类） |
| 金句 | 署名 |
| 长文 | 眉头标签 / 标题 / 署名 |
| 诗词 | 标题 / 作者 / 竖排或横排 / 显示印章 / 显示标点符号 |

### 诗词卡的两个显示开关

右侧「显示」分组里有两个勾选框，**都默认关**：

- **显示印章** — 右下角那枚方章（刻「閒」字，取主题的 `seal` 色）。默认不显示。
- **显示标点符号** — 关掉时按古典竖排惯例隐去句读，只去掉 `，。、；：！？…—`
  这类句读标点，**不动书名号《》与引号**（它们在诗句里是内容的一部分，
  去掉会改变语义）。

标点是在**渲染时**去掉的，编辑区里的原文始终是用户敲进去的样子——
否则一勾「显示标点」就再也找不回来了。导出的 PNG 与屏幕所见一致。

默认关掉让卡片更接近传统竖排诗词的观感；需要保留标点时勾上即可。

### 从原文解析题目 / 作者

诗词 / 长文 / 金句的编辑栏里有 **解析题目 / 作者** 按钮，点一下就从原文里
拆出题头并把它们从正文中移出。识别的是这些写法：

| 写法 | 例子 |
|---|---|
| 书名号 | `《江雪》` |
| 显式标注 | `题目：江雪`、`标题：静夜思` |
| Markdown 标题 | `# 江雪` |
| 一行同时给出 | `江雪 · 柳宗元` |
| 首行短、正文更长 | `江雪` 单独一行 + 后面的诗句 |
| 破折号落款 | `—— 柳宗元` |
| 显式署名 | `作者：柳宗元` |
| 题目下一行 | `江雪` / `柳宗元` / 诗句… |
| 括号署名 | `（唐）王维` |

用本地规则而不是大模型：粘贴后要立刻出结果，规则匹配即时、免费、可离线；
语义层面的理解由方舟拆解负责，两者分工不重叠。

判定原则是**宁可少认，不可错认**——把诗的第一句当成题目，比没解析出题目更糟。
两条关键防线：

- **题目**：拿掉首行后，其余行的平均长度必须**严格大于**首行。这样「每行都是
  5 字」的四句五言不会被误判成「首行是题目」。
- **作者**：题目下面那行也要求后续诗句更长。否则
  `静夜思 / 床前明月光 / 疑是地上霜 / …` 会把「床前明月光」当成作者。

另外破折号规则**不接受单个半角连字符**，因为 `- 第一点` 是 Markdown 列表项，
放宽会把每个列表的第一条当成署名。

解析会改写正文，所以结果条上带一个 **撤销**，一键还原到点击前的状态。

---

## 技术栈

- Vite + React 18 + TypeScript
- Tailwind CSS v3（设计 token 在 `tailwind.config.ts`）
- [Shiki](https://shiki.style/) — 代码高亮（直接用 VS Code TextMate 主题）
- [html-to-image](https://github.com/bubkoo/html-to-image) — DOM → SVG（PNG 栅格化为自研管线，保证字体就绪）
- [@fontsource](https://fontsource.org/) — 字体 self-host
- [react-markdown](https://github.com/remarkjs/react-markdown) + remark-gfm — Markdown 渲染
- highlight.js — 代码语言自动识别
- [火山方舟](https://www.volcengine.com/product/ark) — 诗词拆解（文本模型）与背景图生成（Seedream）

## 项目结构

```
shared/                            前后端共用的领域逻辑
├── scene.ts                       五字段模型 + 提示词拼装（前端也直接用）
├── arkServer.ts                   火山方舟调用（诗文拆解 / 图片生成）
└── handler.ts                     两个 API 路由的共同实现

functions/api/                     Cloudflare Pages Functions（生产环境）
├── dissect.ts                     POST /api/dissect
└── generate-image.ts              POST /api/generate-image

src/
├── App.tsx                       三栏布局 + 状态
├── lib/
│   ├── classifier.ts             内容分类启发式
│   ├── exporter.ts               html-to-image 封装
│   ├── shiki.ts                  Shiki 单例
│   ├── detectLang.ts             代码语言识别
│   ├── arkClient.ts              前端访问 /api/* 的薄封装
│   ├── parsePoem.ts              从原文解析题目 / 作者 / 正文
│   ├── fonts.ts                  可选字体表 + 按需加载字体 CSS
│   ├── zip.ts                    store-only ZIP 打包 + CRC32（零依赖）
│   ├── exportSnapshot.ts         任意快照的离屏导出（单张 / 批量 ZIP）
│   ├── thumb.ts                  240px 缩略图生成
│   ├── storage.ts                存储用量与持久化授权封装
│   ├── format.ts                 时间与体积格式化
│   ├── useHashRoute.ts           #/edit ⇄ #/library
│   ├── snapshots.ts              「最近」的 IndexedDB 存取与淘汰
│   ├── useSnapshots.ts           保存 / 删除 / 清空的状态钩子
│   └── useScene.ts               五字段 + 生成/拆解的状态钩子
├── components/
│   ├── Editor.tsx                左：编辑器 + 标题/作者 + 解析 + 示例 chips
│   ├── Preview.tsx               中：等比缩放预览
│   ├── Controls.tsx              右：尺寸/主题/字体/字段
│   ├── SnapshotCard.tsx          从 SnapshotState 渲染诗词卡（全项目唯一入口）
│   ├── LibraryPage.tsx           管理页：网格/检索/排序/多选/批量导出
│   ├── LibraryCard.tsx           网格与列表里的一张卡片（网格用迷你卡片）
│   ├── SnapshotDetail.tsx        详情灯箱（居中大图 + 操作，元信息默认收起）
│   ├── ScaledCard.tsx            把真实卡片按目标宽度等比缩放（网格/灯箱共用）
│   ├── StorageMeter.tsx          存储占用与持久化授权
│   ├── FontPicker.tsx            右：可折叠的中文/西文字体选择器
│   ├── ScenePanel.tsx            右：AI 背景（五字段 + 拆解/生成）
│   ├── RecentPanel.tsx           「最近保存」列表弹层
│   ├── CardFrame.tsx             导出根节点，统一尺寸
│   └── cards/
│       ├── CodeCard.tsx
│       ├── QuoteCard.tsx
│       ├── ProseCard.tsx
│       └── PoetryCard.tsx
├── themes/                       每种风格各自的主题表
└── styles/index.css              Tailwind + 字体 + 噪点
```

本地开发时 `/api/*` 由 `vite.config.ts` 里的中间件承接，与线上 Pages Functions
共用 `shared/handler.ts`，因此 dev 与线上的校验、错误码、响应形状完全一致。

## 自定义主题

每个 `themes/*.ts` 文件是一个数组，复制一项改色值即可：

```ts
// themes/quoteThemes.ts
{
  name: '夜空',
  background: 'radial-gradient(... ) , linear-gradient(...)',
  text: '#ffffff',
  accent: '#ffd166',
  rule: 'rgba(255,255,255,0.25)',
  quoteMark: 'rgba(255,209,102,0.2)',
  noise: 0.08,
}
```

保存后 dev server 热更新，右侧主题面板会自动出现新项。

## 字体

通过 [@fontsource](https://fontsource.org/) 本地打包（self-host），无任何外部请求，
国内网络可用、可离线运行：

**首屏静态引入（各卡片的默认排版，5 套）**

- **Fraunces Variable** — 英文衬线（金句 / 长文标题，含光学尺寸轴）
- **Inter** — 英文 sans（UI / 署名）
- **JetBrains Mono** — 等宽（代码）
- **Noto Serif SC Variable** — 思源宋体（中文金句 / 诗词 / 长文正文）
- **Ma Shan Zheng** — 楷书（诗词标题）

**可选字体（18 套，选中时才按需加载）**

其余档位见上文「字体」一节，各自是独立的 CSS chunk，不选就不下载。
默认字体必须静态引入，否则首屏会出现字形回退；可选的则不能静态引入，
否则 CSS 会从 269KB 涨到 MB 级。

CJK 字体按 unicode-range 分成上百个小片，浏览器只下载页面实际用到的片。
导出时会把已加载的字体分片内联进图片，**导出结果与屏幕渲染完全一致**。

## 已知限制

- **导出耗时约 1.5–2 秒/张**：导出管线会等待 SVG 内嵌字体就绪并校验渲染稳定，
  这是字体保真的代价。**单张 PNG 实测约 10MB**（`pixelRatio: 3` 下 1080×1440
  变成 3240×4320，噪点层让 PNG 几乎压不动）——所以批量导出很吃内存与时间：
  20 张约 200MB、约 40 秒，ZIP 是在内存里拼好再下载的，不建议一次导几十张。
- **保存不设条数上限**，但配额写满是会失败的：此时会明确报错并指向管理页，
  **不会**自动淘汰旧数据。库越大越应该先在管理页申请「持久化存储」，
  否则磁盘紧张时浏览器可能不经询问清空 IndexedDB。
- **自适应尺寸** 默认 min-height 900px（避免极扁卡片），短内容底部会留白；
  右侧「留白」可切「紧凑」让卡片贴合内容，下限按卡片类型区分（一般 480px、
  代码 680px）；竖排诗词需要高度排列、不参与紧凑（其「留白」开关自动隐藏）。
- **多风格机制仍在代码里但没有入口**：代码 / 金句 / 长文三套卡片、各自的主题表、
  `classifier.ts` 都保留着（`SnapshotCard` 目前只渲染诗词卡）。要恢复多风格，
  在 `SnapshotCard` 里加分支并在 `App.tsx` 把 `FIXED_STYLE` 改回 state 即可。

## License

MIT
